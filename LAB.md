# Τελείες — κλαδί πειραμάτων

Κλαδί για ελεύθερα πειράματα πάνω στο `bars.html`, με τις ρυθμίσεις που
είχε διαλέξει ο χρήστης όταν το παρέδωσε. Το παρακάτω δεν είναι
προδιαγραφή· είναι όσα ήδη δοκιμάστηκαν και τι κόστισε το καθένα, ώστε
να μη χρειαστεί να ξαναανακαλυφθούν.

## Τι είναι

Οπτικοποίηση φωνής για βοηθό αυτοκινήτου, στα ελληνικά. Πέντε πορτοκαλί
τελείες σε μαύρο. Καθαρό Canvas 2D — **καμία εξωτερική βιβλιοθήκη
πουθενά, ούτε WebGL**. Ένα αρχείο, αυτόνομο.

Τρεις καταστάσεις, και όλες κρέμονται από **δύο μόνο μεγέθη**:

| | |
|---|---|
| `level` | 0..1, στιγμιαία ένταση φωνής |
| `mode` | `'idle'` \| `'listen'` \| `'speak'` |

Αργότερα αυτά θα έρχονται από `AnalyserNode` (μικρόφωνο ή ροή του
ElevenLabs). Σήμερα παράγονται από συνθετικό «περίγραμμα φωνής» που
μετράει συλλαβές στο ίδιο το ελληνικό κείμενο. Ό,τι κι αν αλλάξει στη
γεωμετρία, **αυτή η διεπαφή μένει**.

**Σιωπή και ακρόαση:** οι τελείες μένουν τέλειοι κύκλοι και τις διατρέχει
ένα ημίτονο — κάθε μία με δική της φάση, καθυστερημένη κατά σταθερό βήμα.
Κινούνται όλες συνέχεια· ταξιδεύει η κορυφή.

**Ομιλία:** το κύμα σβήνει και οι τελείες ψηλώνουν **όλες μαζί**, η
καθεμιά προς το δικό της ταβάνι. Η σιλουέτα κρατάει πάντα το ίδιο σχήμα —
μεσαία ψηλή, πλαϊνές μεσαίες, ακριανές κοντόχοντρες — και φουσκώνει σαν
ένα σώμα.

## Πού ζει τι

Όλα στο `bars.html`, σε έξι αριθμημένες ενότητες. **Τα πειράματα αφορούν
σχεδόν αποκλειστικά την ενότητα 2.**

| | γραμμή | |
|---|---|---|
| 1. Κατάσταση | 177 | `level`, `mode`, `mixListen`, `mixSpeak`, `glowEnv` |
| **2. Τελείες** | **188** | **όλη η γεωμετρία και η σχεδίαση** |
| 3. Περίγραμμα φωνής | 446 | `syl`, `buildEnv`, `sampleEnv` |
| 4. Σενάριο | 494 | οκτάγυρος διάλογος, TTS |
| 5. Βρόχος | 601 | `loop` |
| 6. Χειριστήρια | 650 | πάνελ, `localStorage`, `window.AIAS` |

Μέσα στην ενότητα 2:

- **`barW(i,n)`** — το βάρος κάθε θέσης, `0.25 + 0.75·συν(u·π/2)²`
- **`barRatio(i)`** — το ταβάνι της θέσης σε διαμέτρους
- **`stepBars(dt,lv,br)`** — προχωράει το κύμα και την κοινή έκταση
- **`layout()`** — διάμετρος, βήμα, θέσεις, ενεργό ταβάνι
- **`paintBars(g,lv,bright)`** — ζωγραφίζει· καλείται δύο φορές ανά καρέ,
  στον ορατό καμβά και στον καμβά λάμψης
- **`composite(lv)`** — η αλυσίδα θολώματος

## Τέσσερα που δεν πρέπει να σπάσουν

Το καθένα κόστισε έναν γύρο.

**1. Οι φάσεις ολοκληρώνονται στον χρόνο.** `φάση += dt · ρυθμός`, ποτέ
`φάση = χρόνος · ρυθμός`. Αν γραφτεί το δεύτερο, κάθε αλλαγή ρυθμού
—από ρυθμιστικό ή από το `mixListen`— μετατοπίζει ακαριαία ολόκληρο το
κύμα. Στο αρχικό σχέδιο με τις κορδέλες αυτό έδινε πήδημα ~11 rad στα 60
δευτερόλεπτα και ο χρήστης το είδε αμέσως.

**2. Η τελεία είναι γραμμή μηδενικού μήκους με στρογγυλό τελείωμα.**

```
μισό ύψος = Δ/2 + έκταση · Δ·(αναλογία−1)/2
```

Με έκταση μηδέν βγαίνει Δ/2, δηλαδή `y0 === y1`, και το `lineCap:'round'`
ζωγραφίζει από μόνο του τέλειο κύκλο διαμέτρου Δ. Δεν είναι προσέγγιση,
είναι ταυτότητα. Υπάρχει ένα `+0.01` στο `y1` επίτηδες: κάποιες μηχανές
παραλείπουν εντελώς διαδρομή μηδενικού μήκους και η τελεία θα
εξαφανιζόταν.

**3. Μία κοινή έκταση, όχι πέντε.** `EXT` είναι **αριθμός**, όχι πίνακας,
και το ίδιο ο κυματισμός. Ο χρήστης ζήτησε ρητά «όλες σαν ένα πράγμα».
Παράπλευρα, από αυτό βγαίνει και η συμμετρία δωρεάν: η πρώτη ταυτίζεται
με την πέμπτη επειδή έχουν ίδιο ταβάνι και τίποτε άλλο δεν τις ξεχωρίζει.

**4. Το φούσκωμα μένει κάτω από το βήμα.** Στο κύμα η τελεία φουσκώνει
κατά 7% (`Di = D*(1+0.07*w)`) ενώ το βήμα είναι 1.08–1.12 διάμετροι. Αν
το φούσκωμα ξεπεράσει το βήμα, οι πυρήνες αλληλοκαλύπτονται και η
προσθετική σύνθεση ανάβει φωτεινή ραφή ανάμεσά τους.

## Ο κανόνας του περιορισμού

Στη `layout()`, ο περιορισμός **πλάτους** σμικρύνει τα πάντα — εκεί δεν
υπάρχει άλλη επιλογή. Ο περιορισμός **ύψους** κόβει το ταβάνι
(`LAY.rmax`) και **ποτέ τη διάμετρο**.

Αυτό ήταν σφάλμα που διορθώθηκε: όσο ο περιορισμός ύψους σμίκρυνε το `D`,
ο ρυθμιστής του ύψους έκανε τις τελείες **μικρότερες** και ο ρυθμιστής
του πάχους δεν έκανε τίποτα πέρα από ένα σημείο. Αν αλλάξει η `layout()`,
να μείνει αυτός ο διαχωρισμός.

Για τον ίδιο λόγο η `showVals()` περνάει από την **ίδια** `layout()` που
ζωγραφίζει: αλλιώς η ένδειξη έλεγε 42% ενώ η οθόνη χωρούσε 26%.

## Η προσθετική σύνθεση ξεπλένει το χρώμα

Ο πυρήνας και η τελική επίστρωση προστίθενται. Όταν το άθροισμα των άλφα
ξεπεράσει περίπου το 1.2, το κόκκινο κορεννύεται στο 255 και ό,τι
περισσεύει ανεβάζει μόνο το πράσινο — το πορτοκαλί γίνεται κίτρινο και
μετά λευκό. Συνέβη τρεις φορές, κάθε φορά που μεγάλωσαν τα σχήματα.
**Αν μεγαλώσουν κι άλλο, το χρώμα θέλει ξαναζύγισμα**, όχι απλώς λιγότερη
λάμψη.

## Η διεπαφή για δοκιμές

```js
window.AIAS.manual('idle'|'listen'|'low'|'mid'|'high')  // κατάσταση με το χέρι
window.AIAS.script()                                     // ο οκτάγυρος διάλογος
window.AIAS.level = 0.7                                  // εξωτερική ένταση
window.AIAS.mode  = 'speak'
window.AIAS.probe()   // {n, D, x0, pitch, cy, rmax, ext, wave[]}
```

Το `probe()` μπήκε ακριβώς για τις μετρήσεις: με τις τελείες να ακουμπάνε
και να σβήνουν εναλλάξ, η αναγνώριση των ορίων από τα εικονοστοιχεία δεν
ήταν πια αξιόπιστη. Δίνει τις θέσεις· τα **ύψη μετριούνται από τα
εικονοστοιχεία** σε κατακόρυφες τομές γύρω από κάθε γνωστό κέντρο.

## Πώς επαληθεύεται

Playwright με το Chromium του περιβάλλοντος. Τέσσερις μετρήσεις που
δούλεψαν, με τους αριθμούς που έβγαλαν:

**Είναι κύκλος;** Σε οριζόντια απόσταση `0.30·Δ` από το κέντρο, ένας
τέλειος κύκλος δίνει ύψος `2·√(0.25−0.09) = 0.800` της διαμέτρου.
Μετρήθηκε **0.811**.

**Κινούνται όλες;** Ποσοστό καρέ όπου κάθε τελεία μετακινήθηκε ≥1 px.
Μετρήθηκε **96–98%** και για τις πέντε. Το υπόλοιπο είναι οι στιγμές που
η τελεία περνάει από το άκρο του ημιτόνου. Ο **παλιός** μηχανισμός με
τους λοφίσκους έδινε 86–89% και διαβαζόταν ως «μία τη φορά» — αν το
νούμερο πέσει, το κύμα έπαψε να είναι κύμα.

**Είναι ένα σώμα;** Λύσε την έκταση **χωριστά από κάθε ύψος**:
`ε = (ύψος/Δ − 1)/(αναλογία − 1)`. Αν κινούνται μαζί, οι πέντε τιμές
συμπίπτουν. Μετρήθηκε διασπορά **0.038**· πάνω από ~0.06 σημαίνει ότι
κάτι τις ξαναχώρισε.

**Το κύμα γύρω από την ισορροπία:** κάθε τελεία 66 px πάνω και 65 κάτω
από τη γραμμή του κέντρου, συνολική διαδρομή **0.80 × Δ**.

Και τα βασικά: **60 fps**, μηδέν σφάλματα κονσόλας, και οι **οκτώ γύροι**
του σεναρίου να παίζουν ως το τέλος.

## Δύο παγίδες στις δοκιμές

**Το `localStorage` υπερισχύει.** Η σελίδα φορτώνει τα ρυθμιστικά από το
`localStorage` με πρόθεμα `bars.`. Σε φυλλομετρητή που την έχει
ξανανοίξει, οι αποθηκευμένες τιμές νικάνε τις προεπιλογές του HTML.
Καθάρισέ το πριν από κάθε μέτρηση.

**Το `file://` δεν αρκεί.** Ο service worker δεν φορτώνει καθόλου εκεί.
Για οτιδήποτε αφορά PWA, cache ή λειτουργία εκτός δικτύου, χρειάζεται
πραγματικό HTTP — αρκεί ένα `python3 -m http.server`.

## Τι δεν αγγίζεται

**Το `v4.html`.** Είναι η σφαίρα με τις κορδέλες, δουλειά πολλών γύρων,
και ο χρήστης το έχει ζητήσει ρητά και επανειλημμένα: *«σε καμία
περίπτωση δε θέλω να χαθεί η προηγούμενη δουλειά που έχουμε κάνει με το
visualization»*. Ίδιο για τα `v1`, `v2`, `v3` — παλιότερες εκδοχές που
κρατιούνται επίτηδες.

Ο δρόμος (`road.html`) βγήκε από την εφαρμογή με απόφαση του χρήστη.
Ζει στα κλαδιά `claude/road-visualizer` και `claude/android-auto`.

## Οι ρυθμίσεις που παραλαμβάνεις

| ρυθμιστικό | τιμή | |
|---|---|---|
| Πόσες | 5 | |
| Μέγεθος τελείας | 1.84 | ≈ 35% της μικρής πλευράς |
| Ύψος στην ομιλία | 9 | κορεννύεται στο 90% της οθόνης |
| Απόσταση | 1.08 × Δ | ελάχιστο — σχεδόν ακουμπάνε |
| Ρυθμός | 1.00× | |
| Ένταση κύματος | 0.30 × Δ | |
| Λάμψη · Απαλότητα | 1.00 · 0.35 | |

Το «9» στο ύψος είναι σκόπιμο: σημαίνει «όσο ψηλά χωράει σε αυτή την
οθόνη». Το πάνελ το δείχνει ως «2.5 × Δ · 90% (στο όριο)».
